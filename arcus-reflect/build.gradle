
dependencies {

    [
            project(':arcus-lang'),
            project(':arcus-lambda'),
            'net.bytebuddy:byte-buddy',
    ].each { dependency ->
        implementation dependency
    }

    compileOnly project(':annotations:arcus-jsr-305')
}