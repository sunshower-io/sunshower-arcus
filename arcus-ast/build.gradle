dependencies {

    [
            ':arcus-lang',
            ':arcus-lambda',
            ':arcus-reflect',
            ':annotations:arcus-jsr-305'
    ].each {
        implementation project(it)
    }
}