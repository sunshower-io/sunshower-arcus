
dependencies {

    [
            project(':lang'),
            project(':lambda'),
            'net.bytebuddy:byte-buddy',
    ].each { dependency ->
        implementation dependency
    }

    compileOnly project(':annotations:305')
}