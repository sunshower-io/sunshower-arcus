persistence:
  data-sources:
    - data-source:
        modes:
          - read
        name: first-datasource
        url: jdbc:hd:mem:first
    - data-source:
        modes:
          - read
          - write
        name: first-datasource
        url: jdbc:hd:mem:first
